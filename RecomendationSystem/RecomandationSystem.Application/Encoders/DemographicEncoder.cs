using RecomandationSystem.Application.Enums;
using RecomandationSystem.Application.Models;

namespace RecomandationSystem.Application.Encoders
{
    public sealed class DemographicEncoder
    {
        //public float[] Encode(User user)
        //{
        //    var features = new List<float>();

        //    features.Add((user.Age - 12f) / 88f);

        //    features.AddRange(new[]
        //    {
        //        user.UserGender == Gender.Male ? 1f : 0f,
        //        user.UserGender == Gender.Female ? 1f : 0f,
        //        user.UserGender == Gender.Other ? 1f : 0f
        //    });


        //    return features.ToArray();
        //}
    }
}
